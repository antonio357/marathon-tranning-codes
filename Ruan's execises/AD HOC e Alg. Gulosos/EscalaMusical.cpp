#include <iostream>

using namespace std;
// notas musicais
int main() {
    int* l;
    int n;
    while (scanf("%d", &n) != EOF) {
        l = new int[n];
        for (int i = 0; i < n; i++) scanf("%d", &l[i]);

    }
    return 0;
}